<h1>Editando artigo existente</h1>
<% if @artigo.errors.any? %>
<h2>Os seguintes erros estão prevenindo o artigo de ser criado</h2>
	<ul>
		<% @artigo.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
	</ul>
<% end %>

<%= form_for @artigo do |f| %>
	<p>
		<%= f.label :titulo %><br/>
		<%= f.text_field :titulo%>
	</p>
	<p>
		<%= f.label :descricao %><br/>
		<%= f.text_area :descricao %>
	</p>
	<p>
		<%= f.submit%> 
	</p>

<% end %>

<%= link_to "Voltar para os artigos", artigos_path %>